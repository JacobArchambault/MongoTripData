# MongoTripData-type-module-
A node, express, and handlebars application retrieving trip data from a Mongo NoSQL database

## How to run
Open up a terminal in the root of the application, then type:

`DEBUG=myapp:* npm start`
